1.新增-比對結果時間，就是執行步驟1的時間，顯示在DATAGIRDVIEW下方
然後步驟2更新完成後 不用自動去比對
讓使用者自行在按BUTTON1

2.連線字串
參考前輩:
   private static void OpenSqlConnectionLocalhost(SqlConnection connection)
        {
            string connectionString = GetConnectionStringLocal();
            connection.ConnectionString = connectionString;
            connection.Open();
        }
        static private string GetConnectionStringLocal()
        {
            return "Data Source=localhost;Initial Catalog=POSSC;User ID=sa;Password=23285582";
        }
        private void button3_Click(object sender, EventArgs e)
        {
            SqlConnection sConn = new SqlConnection();
            OpenSqlConnectionLocalhost(sConn);
            DataTable dt = new DataTable();
            string sql = "DELETE FROM P_PLU ";
            SqlDataAdapter Tb = new SqlDataAdapter(sql, sConn);
            Tb.Fill(dt);
            MessageBox.Show("作業完成");
        }

有在測試環境的資料庫hilife-tfs(SQL Server 15.0.4261.1 - pos)
的[HI_TMMAIN].[dbo].[FileModList]
這樣要怎麼改?
